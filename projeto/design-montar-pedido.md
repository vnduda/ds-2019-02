Classes: 
    Pedido -> métodos:
                recebeRequisicao()
                acrescentaItem()
    EntregaDoPedido -> métodos:
                                defineEnderecoLoja()
                                registraEnderecoDestino()
                                calculaFrete()
    CalculaPedido -> métodos:
                               calculaTotalPedido()